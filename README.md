# PROD 2026 - Backend

# Система A/B тестирования

## Runbook

| ID задания | ID критерия | Проблема/риск | Где реализовано                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Как проверяется                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Какие данные нужны                                                                                                                                                                                                                                                                                                                                                         | Статус |
|------------|-------------|---------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------|
| `3.4`      | `B2-1` | При отсутствии активного эксперимента система возвращает не `default`, из-за чего ломается контрольный сценарий и сравнение вариантов. | [decisions_service.py](ab_test_platform/src/application/decisions_service.py) метод `make_decision`: `get_active_experiment_for_flag` возвращает `None` → `_return_default_without_experiment`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `POST /api/v1/decision` с `flags_keys` для флага без активного эксперимента; ожидается `value = <default_value флага>` и `id = null`.                                                                                                                                                                                                                                                                                                                                                                                         | Флаг с установленным `default_value`; ни одного эксперимента в статусе `RUNNING`/`PAUSED` на этом флаге; любой `subject_id`.                                                                                                                                                                                                                                               | `реализовано` |
| `2.7`      | `B2-2` | Пользователь, не соответствующий правилу таргетинга, получает вариант вместо `default`, что искажает выборку. | [decisions_service.py](ab_test_platform/src/application/decisions_service.py) метод `make_decision`: `parser.check_rule_matches(subject_attr, targeting_rule)` возвращает `False` → `_return_default_without_experiment`. DSL-парсер: [dsl_parser/](ab_test_platform/src/infra/utils/dsl_parser/).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `POST /api/v1/decision` с двумя субъектами: один удовлетворяет правилу таргетинга (ожидается вариант + ненулевой `id`), второй — нет (ожидается `default` + `id = null`).                                                                                                                                                                                                                                                                                                                                                     | Флаг + эксперимент `RUNNING` с долей аудитории 100% и правилом таргетинга (например, `country == "RU"`); субъект с атрибутом, проходящим правило, и субъект без нужного атрибута/с другим значением.                                                                                                                                                                       | `реализовано` |
| `3.4`      | `B2-3` | При применимом эксперименте система возвращает `default` вместо варианта. | [decisions_service.py](ab_test_platform/src/application/decisions_service.py) метод `_make_ab_decision`: `_hash_bucket` + `_pick_variant` выбирают вариант и сохраняют `Decisions` в БД; результат возвращается с ненулевым `id`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `POST /api/v1/decision` для флага с `RUNNING`-экспериментом, 100% аудитории, без таргетинга (или субъект проходит таргетинг); ожидается значение одного из вариантов и ненулевой `decision_id`.                                                                                                                                                                                                                                                                                                                               | Флаг + эксперимент `RUNNING` с ≥1 вариантом и суммой весов = 100; субъект, не участвовавший недавно в экспериментах (нет cooldown / лимита активных экспериментов).                                                                                                                                                                                                        | `реализовано` |
| `3.5.1`    | `B2-4` | При неизменной конфигурации повторный запрос того же субъекта возвращает другой вариант, что ломает статистику и UX. | [decisions_service.py](ab_test_platform/src/application/decisions_service.py) метод `_hash_bucket`: детерминированный SHA-256 хеш `"{subject_id}:{experiment_id}" % 100`; stickiness: при повторном запросе берётся уже сохранённый `Decisions` из БД (`get_decision_by_subject_and_experiment`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Выполнить `POST /api/v1/decision` с одним и тем же `subject_id` и `flags_keys` дважды и более при неизменной конфигурации; оба ответа должны содержать идентичные `value` и `id`.                                                                                                                                                                                                                                                                                                                                             | Флаг + `RUNNING`-эксперимент; фиксированный `subject_id`, которому уже было выдано решение (`id = null` — тоже валидное детерминированное решение: пользователь не попал в эксперимент по хешу, и это воспроизводимо).                                                                                                                                                     | `реализовано` |
| `2.2`      | `B2-5` | Фактическое распределение субъектов по вариантам систематически не соответствует заданным весам. | [decisions_service.py](ab_test_platform/src/application/decisions_service.py) метод `_pick_variant`: кумулятивное суммирование весов — бакет попадает в первый вариант, чей накопленный вес превышает `bucket % 100`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Запустить сценарий в эмуляторе с ≥500 субъектами для эксперимента с известными весами (например, 50/50 или 70/30); проверить в [Metabase](http://localhost:3000) или `GET /api/v1/experiments/{id}/report` — отклонение от заданных весов должно быть в пределах статистической погрешности.                                                                                                                                                                                                                                  | Флаг + `RUNNING`-эксперимент с ≥2 вариантами с явно заданными весами; конфиг сценария эмулятора (`use_real_time=false`, `subjects_count ≥ 500`).                                                                                                                                                                                                                           | `реализовано` |
| `2.4`      | `B3-1` | Невозможность перевести эксперимент на ревью блокирует процесс согласования и запуска. | [experiment_service.py](ab_test_platform/src/application/experiment_service.py) метод `submit_for_review` → `_transition(experiment_id, REVIEW, actor_id)`; допустимые переходы: [experiments.py](ab_test_platform/src/models/experiments.py) `ALLOWED_TRANSITIONS`. Эндпоинт: [routes/experiments.py](ab_test_platform/src/api/routes/experiments.py) `POST /experiments/{id}/submit`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `POST /api/v1/experiments/{id}/submit` для эксперимента в статусе `DRAFT`; ожидается `status = "REVIEW"`. Повторный вызов (`REVIEW → REVIEW`) должен вернуть ошибку.                                                                                                                                                                                                                                                                                                                                                          | Эксперимент в статусе `DRAFT`; пользователь с ролью `EXPERIMENTER` (который этот эксперимент создал) или `ADMIN`.                                                                                                                                                                                                                                                          | `реализовано` |
| `2.4`      | `B3-2` | При достижении порога одобрений статус не переходит в `APPROVED`, блокируя запуск. | [reviews_service.py](ab_test_platform/src/application/reviews_service.py) метод `create_review`: после `ACCEPT`-ревью считается `count_by_decision(ACCEPT)` и сравнивается с `approve_group.min_approvals`; при достижении вызывается `transition_status(APPROVED)`. После REJECT ревью эксперимент уходит в отклоненные, после REQUEST_IMPROVEMENTS возвращается к черновику. Эндпоинт: [routes/reviews.py](ab_test_platform/src/api/routes/reviews.py) `POST /experiments/{id}/review`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `POST /api/v1/experiments/{id}/review` с `decision = "ACCEPT"` нужное число раз от тех пользователей, что находятся в approver группе экспериментатора (по `min_approvals` группы); после последнего одобрения `GET /api/v1/experiments/{id}` должен вернуть `status = "APPROVED"`.                                                                                                                                                                                                                                           | Эксперимент в статусе `REVIEW`; аппрувер-группа с `min_approvals ≥ 1`; пользователи с ролью `APPROVER`, входящие в группу аппруверов для экспериментатора, создавшего эксперимент, или если группа не задана вручную - то просто любые пользователи       `APPROVER`  (по умолчанию группа не задана - значит одобрять могут все)                                          | `реализовано` |
| `2.5`      | `B3-3` | Эксперимент запускается без нужного числа одобрений, что отменяет смысл ревью. | [experiment_service.py](ab_test_platform/src/application/experiment_service.py) метод `start_experiment` → `_transition(RUNNING)`; старт разрешён только из `APPROVED` (см. `ALLOWED_TRANSITIONS`). Статус `APPROVED` выставляется в [reviews_service.py](ab_test_platform/src/application/reviews_service.py) только после набора `min_approvals`. Эндпоинт: `POST /experiments/{id}/start`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Попытка `POST /api/v1/experiments/{id}/start` для эксперимента в статусе `REVIEW` или `DRAFT` должна вернуть ошибку `409 Conflict`. Старт проходит только после `APPROVED`.                                                                                                                                                                                                                                                                                                                                                   | Эксперимент в статусе `REVIEW` (не набравший нужного числа одобрений); пользователь с ролью `EXPERIMENTER`/`ADMIN`.                                                                                                                                                                                                                                                        | `реализовано` |
| `2.4`      | `B3-4` | Произвольные переходы статусов нарушают целостность жизненного цикла эксперимента. | [experiment_service.py](ab_test_platform/src/application/experiment_service.py) метод `_transition`: проверяет `target_status not in ALLOWED_TRANSITIONS[current_status]` → `ConflictError`. Граф переходов: [experiments.py](ab_test_platform/src/models/experiments.py) `ALLOWED_TRANSITIONS`. Заморозка конфига: `FROZEN_STATUSES = {RUNNING, PAUSED}` блокирует редактирование.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Попытки недопустимых переходов (`DRAFT → RUNNING`, `APPROVED → DRAFT`, `ARCHIVED → DRAFT` и т.д.) должны возвращать `409 Conflict`. Попытка изменить варианты/веса эксперимента в статусе `RUNNING` / `PAUSED` должна вернуть ошибку.                                                                                                                                                                                                                                                                                         | Эксперименты в разных статусах; набор запросов с явно запрещёнными переходами.                                                                                                                                                                                                                                                                                             | `реализовано` |
| `0.3`      | `B3-5` | Неназначенный пользователь может одобрить эксперимент, нарушая контроль доступа. | [reviews_service.py](ab_test_platform/src/application/reviews_service.py) метод `create_review`: проверяет вхождение `reviewer_id` в `members` аппрувер-группы → `AccessDeniedError` при несоответствии. Если группа пустая (не назначена) -> fallback, проверка не происходит, аппрувать может любой `APPROVER`.  Эндпоинт защищён ролью `APPROVER`/`ADMIN` через `require_roles` в [routes/reviews.py](ab_test_platform/src/api/routes/reviews.py). Управление группами: [approve_groups_service.py](ab_test_platform/src/application/approve_groups_service.py).                                                                                                                                                                                                                                                                                                                                                                                                                                    | `POST /api/v1/experiments/{id}/review` от пользователя `APPROVER`, не входящего в аппрувер-группу экспериментатора, должен вернуть `403 Forbidden`. Пользователь с ролью `EXPERIMENTER` или `VIEWER` не может вызвать этот эндпоинт вообще (`403`).                                                                                                                                                                                                                                                                           | Экспериментатор с явно настроенной аппрувер-группой (1 аппрувер и больше в группе); аппрувер из группы (успешный кейс) и аппрувер вне группы (негативный кейс).                                                                                                                                                                                                            | `реализовано` |
| `4.2` `4.3` | `B4-1` | Событие с неверным типом поля в `payload` принимается, засоряя аналитику невалидными данными. | [events_sevice.py](ab_test_platform/src/application/events_sevice.py) метод `_validate_payload`: для каждого поля из `event_type.payload_schema` проверяет тип значения (`string`→`str`, `number`→`int/float`, `bool`→`bool`); при несоответствии — `rejected_reason=INVALID_PAYLOAD`, `status_code=422` (в мульти статусе). Схема задаётся при создании типа события: [schemas/events.py](ab_test_platform/src/schemas/events.py) `EventTypesCreate.payload_schema: dict[str, str]`, хранится в [models/events.py](ab_test_platform/src/models/events.py) `EventTypes.payload_schema` (JSON).                                                                                                                                                                                                                                                                                                                                                                                                         | `POST /api/v1/events` с событием, у которого поле `payload` содержит значение неверного типа (например, `latency_ms: "fast"` вместо числа); ожидается `status_code=422` в ответе по этому элементу в мульти статусе.                                                                                                                                                                                                                                                                                                          | Тип события с заполненным `payload_schema` (например, `{"latency_ms": "number"}`); событие с некорректным типом поля в `payload`.                                                                                                                                                                                                                                          | `реализовано` |
| `4.2` `4.3` | `B4-2` | Событие без обязательных полей `payload` принимается, делая метрики неполными. | [events_sevice.py](ab_test_platform/src/application/events_sevice.py) метод `_validate_payload`: если поле из `payload_schema` отсутствует в `payload` — ошибка `missing required field`; если `payload` пустой при непустой схеме -- ошибка. Базовые обязательные поля схемы события (`event_type`, `decision_id`) валидируются Pydantic на уровне HTTP-запроса (`422` при отсутствии).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `POST /api/v1/events` с событием без обязательного поля из `payload_schema` (например, `payload: {}` при `payload_schema: {"latency_ms": "number"}`); ожидается `status_code=422` **в теле ответа**. Отправить запрос без `event_type` или `decision_id` — ожидается `422` от FastAPI.                                                                                                                                                                                                                                        | Тип события с непустым `payload_schema`; событие с отсутствующим полем из схемы.                                                                                                                                                                                                                                                                                           | `реализовано` |
| `4.3` | `B4-3` | Дубликат события учитывается как новое, искажая все метрики. | [events_sevice.py](ab_test_platform/src/application/events_sevice.py) шаг 3: `get_non_rejected_raw_event_by_decision_and_type` ищет запись в `events_raw` с `status != REJECTED` по ключу `(decision_id, event_type_id)`; при нахождении — `rejected_reason=DUPLICATE`, `status_code=409`. Проверка по `events_raw` охватывает как `RECEIVED`, так и `PENDING`-события, устраняя пропуск дублей в сценарии out-of-order доставки.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `POST /api/v1/events` дважды с одинаковыми `decision_id` и `event_type`; первый запрос должен вернуть `201`, второй — `409` в ответе по этому элементу. Проверить, что в отчёте значение метрики не удвоилось. Также можно проверить в metabase что событие отклонено с причиной дублирование (+1 отклоненное соытие на диаграмме с причинами отклонения)                                                                                                                                                                     | Любой ненулевой `decision_id`; одинаковый `event_type` в двух последовательных запросах.                                                                                                                                                                                                                                                                                   | `реализовано` |
| `4.4` | `B4-4` | Экспозиция не привязана к `decision_id`, что делает атрибуцию конверсий невозможной. | Каждое событие принимается только при валидном `decision_id` (шаг 2 в [events_sevice.py](ab_test_platform/src/application/events_sevice.py)): `decisions_repository.get(decision_id)` → `EntityNotFoundError` при отсутствии → `rejected_reason=INVALID_DECISION_ID`. Поля `decision_id` и `subject_id` хранятся в обеих таблицах: [models/events.py](ab_test_platform/src/models/events.py) `Events` и `EventsRaw`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `POST /api/v1/events` с несуществующим `decision_id`; ожидается `status_code=404`. Для валидного `decision_id` — проверить что в `events_raw`/`events` сохранены `decision_id` и `subject_id`, проверить что конверсия                                                                                                                                                                                                                                                                                                        | Существующий и несуществующий `decision_id`; любой зарегистрированный `event_type`.                                                                                                                                                                                                                                                                                        | `реализовано` |
| `4.4` | `B4-5` | Конверсия без факта экспозиции засчитывается, занижая реальную конверсию и завышая метрику. | [events_sevice.py](ab_test_platform/src/application/events_sevice.py) метод `_process_dependent_event`: если `event_type.requires_event_id` задан, проверяется `has_fulfilled(decision_id, requires_event_id)` в Redis (то есть ищутся события-экспозиции которые не зависят от других событий); при отсутствии экспозиции — событие уходит в `PENDING` (`EventsStatus.PENDING`) и не попадает в таблицу `events` (не учитывается в продуктовых метриках). Когда экспозиция приходит позже, `_process_independent_event` вызывает `pop_pending` и переносит ожидавшее событие в `events`, а также обновляет статус события в events_raw. TTL Redis-ключей по дефолту 30 дней (`settings.redis_events_ttl_seconds`) но в env файле можно задать любое. **Ограничение:** fulfilled-состояние хранится только в Redis; при потере данных Redis конверсии, пришедшие после рестарта, не найдут экспозицию и останутся в `PENDING` навсегда. | 1) Получить значение флага и decision_id `POST /api/v1/decisions` 2) `POST /api/v1/events/batch` Отправить событие действия без предшествующей экспозиции → ожидается `status_code=202` (PENDING). Проверить отчёт — действие не учтено (или проверить, что в диаграмме metabase прибавилось pending событие). 3) Затем отправить экспозицию для этого действия → ожидается `201`. Проверить отчёт — действие теперь учтено (метрика "количество действий" в отчете изменилась на +1). Отправлять все по одному `decision_id` | Эксперимент на 100% аудиториии с метриками подсчета событий экспозиции и действия, подсчета конверсии, чтобы получить `decision_id` и сохранение decision, а также чтобы посмотреть в отчете количество событий. Тип события действия с `requires_event_type = <тип экспозиции>`; тип события экспозиции (без зависимостей); один и тот же `decision_id` в обоих событиях. | `реализовано` |