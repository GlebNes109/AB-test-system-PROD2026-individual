# PROD 2026 - Backend

# Система A/B тестирования

## Runbook

| ID задания | ID критерия | Проблема/риск | Где реализовано | Как проверяется | Какие данные нужны | Статус |
|------------|-------------|---------------|-----------------|-----------------|-------------------|--------|
| `3.4` | `B2-1` | При отсутствии активного эксперимента система возвращает не `default`, из-за чего ломается контрольный сценарий и сравнение вариантов. | `ab_test_platform/src/application/decisions_service.py` → `make_decision`: `get_active_experiment_for_flag` возвращает `None` → `_return_default_without_experiment`. | `POST /api/v1/decision` с `flags_keys` для флага без активного эксперимента; ожидается `value = <default_value флага>` и `id = null`. | Флаг с установленным `default_value`; ни одного эксперимента в статусе `RUNNING`/`PAUSED` на этом флаге; любой `subject_id`. | `реализовано` |
| `2.7` | `B2-2` | Пользователь, не соответствующий правилу таргетинга, получает вариант вместо `default`, что искажает выборку. | `ab_test_platform/src/application/decisions_service.py` → `make_decision`: `parser.check_rule_matches(subject_attr, targeting_rule)` возвращает `False` → `_return_default_without_experiment`. DSL-парсер: `ab_test_platform/src/infra/utils/dsl_parser/`. | `POST /api/v1/decision` с двумя субъектами: один удовлетворяет правилу таргетинга (ожидается вариант + ненулевой `id`), второй — нет (ожидается `default` + `id = null`). | Флаг + эксперимент `RUNNING` с долей аудитории 100% и правилом таргетинга (например, `country == "RU"`); субъект с атрибутом, проходящим правило, и субъект без нужного атрибута/с другим значением. | `реализовано` |
| `3.4` | `B2-3` | При применимом эксперименте система возвращает `default` вместо варианта. | `ab_test_platform/src/application/decisions_service.py` → `_make_ab_decision`: `_hash_bucket` + `_pick_variant` выбирают вариант и сохраняют `Decisions` в БД; результат возвращается с ненулевым `id`. | `POST /api/v1/decision` для флага с `RUNNING`-экспериментом, 100% аудитории, без таргетинга (или субъект проходит таргетинг); ожидается значение одного из вариантов и ненулевой `decision_id`. | Флаг + эксперимент `RUNNING` с ≥1 вариантом и суммой весов = 100; субъект, первый раз обращающийся к этому флагу (нет cooldown / лимита активных экспериментов). | `реализовано` |
| `3.5.1` | `B2-4` | При неизменной конфигурации повторный запрос того же субъекта возвращает другой вариант, что ломает статистику и UX. | `ab_test_platform/src/application/decisions_service.py` → `_hash_bucket`: детерминированный SHA-256 хеш `"{subject_id}:{experiment_id}" % 100`; stickiness: при повторном запросе берётся уже сохранённый `Decisions` из БД (`get_decision_by_subject_and_experiment`). | Выполнить `POST /api/v1/decision` с одним и тем же `subject_id` и `flags_keys` дважды и более при неизменной конфигурации; оба ответа должны содержать идентичные `value` и `id`. | Флаг + `RUNNING`-эксперимент; фиксированный `subject_id`, которому уже было выдано решение (первый запрос его создаёт, повторные — возвращают то же). | `реализовано` |
| `2.2` | `B2-5` | Фактическое распределение субъектов по вариантам систематически не соответствует заданным весам. | `ab_test_platform/src/application/decisions_service.py` → `_pick_variant`: кумулятивное суммирование весов — бакет попадает в первый вариант, чей накопленный вес превышает `bucket % 100`. | Запустить сценарий в эмуляторе с ≥500 субъектами для эксперимента с известными весами (например, 50/50 или 70/30); проверить в Metabase или `GET /api/v1/experiments/{id}/report` фактическое распределение — отклонение от заданных весов должно быть в пределах статистической погрешности. | Флаг + `RUNNING`-эксперимент с ≥2 вариантами с явно заданными весами; конфиг сценария эмулятора (`use_real_time=false`, `subjects_count ≥ 500`). | `реализовано` |
