# Runbook
## Инструкция по старту и общему взаимодействию с сервисом
Перед началом перейдите в корень проекта и выполните команду чтобы скопировать env переменные
```bash
cp .env.example .env
```

Чтобы поднять платформу выполните команду
```bash
docker compose up -d
```
Для скачивания сторонних образов (redis, postgres, metabase итд) при первом запуске потребуется интернет

После старта на 80 порту будет работать nginx, который распределяет запросы по сервисам
* http://localhost - платформа A/B тестирования (главный сервис, решение кейса)
* http://localhost/emulator - система-эмулятор как отдельный сервис в докер-контейнере. Эмулирует внешнюю систему с пользователями, на которых проводится тест. Нужна для прогона сложных сценариев (тест подсчета метрик, guardrails и так далее) с разными вероятностями разных событий. По сути система эмулятор - это многофункциональный скрипт для генерации данных по конфигу.
* http://localhost/metabase - UI визуализация метрик, доп фича

Все сервисы внутри общаются через docker сеть.

Для генерации тестовых данных используются python скрипты которые вызывают api, их надо запускать локально.

> Учетные данные для тестирования (по умолчанию)

Учетка админа в сервисе
```
admin@mail.ru
123123123aA!
```

Учетка в metabase (доп фича)
```
admin@test.com
metabase12
```

---
Что такое система-эмулятор
---
Это отдельный сервис со своим API, который собирается в отдельном докер контейнере и доступен на адресе base_url/emulator. Его сваггер доступен на /emulator/docs. В эмуляторе можно создавать гибкие сценарии тестирования с большими данными и задержками по времени. Эмулятор делает запросы в основную систему через docker сеть.


## Happy-path
Запустите скрипт генерации тестовых данных
```bash
python seed_demo_happypath.py
```

---
Проверьте метрики и логи контейнера. Запросите фичу button-color вручную чтобы убедиться что выдача работает (POST api/v1/decision)

Отправьте события вручную (POST api/v1/events/batch) чтобы убедиться что события принимаются (event type ставьте exposure или click - из тестовых данных seed_demo). Опционально вы можете сгенерировать большой объем данных в эмуляторе чтобы посмотреть что все точно работает.

--- 

Создайте и запустите сценарий в эмуляторе:

```
{
  "scenario_name": "demo button-color",
  "subjects_count": 200,
  "experiment": {
    "feature_flag_key": "button-color",
    "time_delay_seconds": 2,
    "time_variation": 1,
    "variants": [
      {
        "feature_flag_value": "white",
        "events": [
          {"event_type": "exposure", "time_delay_seconds": 0, "time_variation": 0, "probability": 1},
          {"event_type": "click", "time_delay_seconds": 1, "time_variation": 0, "probability": 0.3}
        ]
      },
      {
        "feature_flag_value": "black",
        "events": [
          {"event_type": "exposure", "time_delay_seconds": 0, "time_variation": 0, "probability": 1},
          {"event_type": "click", "time_delay_seconds": 1, "time_variation": 0, "probability": 0.8}
        ]
      }
    ]
  }
}
```



Посмотрите метрики эксперимента в `GET api/v1/experiments/{exp_id_из_вывода_seed_demo}/reports`

Чтобы посмотреть таймсерии задайте в эмуляторе time_delay_seconds (задержку по времени) и посмотрите с гранулярностью минута в `GET api/v1/experiments/{exp_id_из_вывода_seed_demo}/reports/timeseries`. В таком случае сценарий в эмуляторе не прогонится мгновенно (актуальный статус по сценарию эмулятора можно получить в get `/scenarios/{id сценария}` )


