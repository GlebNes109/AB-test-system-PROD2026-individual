info:
  name: "25. EXP_1: Review → APPROVED"
  type: http
  seq: 25

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_1_ID}}/review"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{APPROVER_TOKEN}}
  body:
    type: json
    data: |-
      {
        "decision": "ACCEPT",
        "comment": "Seed FX-1-2."
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test('Status 200', function() { expect(res.getStatus()).to.equal(200); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
