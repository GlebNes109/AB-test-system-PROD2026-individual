info:
  name: "54. EXP_6: Submit → REVIEW"
  type: http
  seq: 54

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_6_ID}}/submit"
  headers:
    - name: Authorization
      value: Bearer {{EXPERIMENTER_TOKEN}}
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test('Status 200', function() { expect(res.getStatus()).to.equal(200); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
