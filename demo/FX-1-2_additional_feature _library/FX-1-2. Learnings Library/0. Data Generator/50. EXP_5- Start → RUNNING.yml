info:
  name: "50. EXP_5: Start → RUNNING"
  type: http
  seq: 50

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_5_ID}}/start"
  headers:
    - name: Authorization
      value: Bearer {{EXPERIMENTER_TOKEN}}
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test('Status 200', function() { expect(res.getStatus()).to.equal(200); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
