info:
  name: "FX-1b: GET /learnings?page=0&size=3 (первая страница)"
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/learnings?page=0&size=3"
  headers:
    - name: Authorization
      value: Bearer {{EXPERIMENTER_TOKEN}}
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "3"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test('Status 200', function() { expect(res.getStatus()).to.equal(200); });
        var json = res.getBody();
        test('items = 3 (первая страница из 3)', function() { expect(json.items).to.have.lengthOf(3); });
        test('total по-прежнему 8', function() { expect(json.total).to.eql(8); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
