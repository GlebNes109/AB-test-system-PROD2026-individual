info:
  name: "B6-5a: Finish БЕЗ result_description (→ 422)"
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_REPORTS_ID}}/finish"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{EXPERIMENTER_TOKEN}}
  body:
    type: json
    data: |-
      {
        "result": "NO_EFFECT"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test('422 — result_description обязателен', function () {
            expect(res.getStatus()).to.equal(422);
        });
        console.log('B6-5a: 422 — поле result_description обязательно');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
