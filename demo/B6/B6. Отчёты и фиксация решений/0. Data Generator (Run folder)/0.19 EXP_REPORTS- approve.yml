info:
  name: "0.19 EXP_REPORTS: approve"
  type: http
  seq: 19

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_REPORTS_ID}}/review"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{APPROVER_TOKEN}}
  body:
    type: json
    data: '{"decision": "ACCEPT", "comment": "Seed B6"}'
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test('200', function () { expect(res.getStatus()).to.equal(200); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
