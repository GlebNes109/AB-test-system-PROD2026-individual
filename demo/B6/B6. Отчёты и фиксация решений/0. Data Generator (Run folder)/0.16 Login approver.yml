info:
  name: 0.16 Login approver
  type: http
  seq: 16

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/auth/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "b6_approver@demo.com",
        "password": "Demo1234!x"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test('200', function () { expect(res.getStatus()).to.equal(200); });
        bru.setVar('APPROVER_TOKEN', res.getBody().accessToken);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
