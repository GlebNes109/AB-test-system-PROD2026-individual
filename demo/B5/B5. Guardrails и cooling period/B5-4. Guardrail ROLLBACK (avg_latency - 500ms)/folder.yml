info:
  name: B5-4. Guardrail ROLLBACK (avg_latency > 500ms)
  type: folder
  seq: 5

request:
  auth: inherit

docs:
  content: |-
    Отправляем latency event с ms=1200 → эксперимент автоматически финишируется с ROLLBACK.
    Подождите ~10 сек между отправкой события и проверкой статуса.
  type: text/markdown
