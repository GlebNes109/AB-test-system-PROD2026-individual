info:
  name: "B5-5a: Triggers для EXP_PAUSE"
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_PAUSE_ID}}/guardrail-triggers"
  headers:
    - name: Authorization
      value: Bearer {{EXPERIMENTER_TOKEN}}
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test('200', function () { expect(res.getStatus()).to.equal(200); });
        var json = res.getBody();
        test('Есть хотя бы 1 trigger', function () { expect(json.length).to.be.above(0); });
        var t = json[0];
        test('threshold = 0.5', function () { expect(t.threshold).to.eql(0.5); });
        test('actual_value > threshold', function () { expect(t.actual_value).to.be.above(0.5); });
        test('action_taken = PAUSE', function () { expect(t.action_taken).to.eql('PAUSE'); });
        console.log('B5-5a: PAUSE trigger — actual=' + t.actual_value + ' > threshold=' + t.threshold);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
