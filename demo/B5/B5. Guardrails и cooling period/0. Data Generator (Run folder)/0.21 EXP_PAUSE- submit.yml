info:
  name: "0.21 EXP_PAUSE: submit"
  type: http
  seq: 21

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_PAUSE_ID}}/submit"
  headers:
    - name: Authorization
      value: Bearer {{EXPERIMENTER_TOKEN}}
  body:
    type: text
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test('200', function () { expect(res.getStatus()).to.equal(200); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
