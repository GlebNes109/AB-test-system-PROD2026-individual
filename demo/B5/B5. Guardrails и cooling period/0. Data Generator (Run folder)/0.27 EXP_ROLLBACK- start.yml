info:
  name: "0.27 EXP_ROLLBACK: start"
  type: http
  seq: 27

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_ROLLBACK_ID}}/start"
  headers:
    - name: Authorization
      value: Bearer {{EXPERIMENTER_TOKEN}}
  body:
    type: text
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test('200', function () { expect(res.getStatus()).to.equal(200); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
