info:
  name: "B3-2c: Второе одобрение (approver_beta)"
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/experiments/{{EXP_APPROVAL}}/review"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{APPROVER_BETA_TOKEN}}
  body:
    type: json
    data: |-
      {
        "decision": "ACCEPT",
        "comment": "Подтверждаю — Beta"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test('Status 200', function () { expect(res.getStatus()).to.equal(200); });
        console.log('B3-2: Второе одобрение от Beta принято');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
