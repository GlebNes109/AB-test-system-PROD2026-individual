services:
  # ── DEV profile ──────────────────────────────────────────────
  postgres:
    image: postgres:15-alpine
    container_name: abtest-postgres
    profiles: [dev]
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d testdb -h 127.0.0.1 -p 5432"]
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 10s

  api:
    build: ./ab_test_platform
    profiles: [dev]
    ports:
      - "8080:8080"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: testdb
      DB_USER: postgres
      DB_PASSWORD: postgres
    depends_on:
      postgres:
        condition: service_healthy

  pgadmin:
    image: dpage/pgadmin4:latest
    profiles: [dev, emulator]
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_LISTEN_PORT: 5050
    ports:
      - "5050:5050"

  metabase:
    image: metabase/metabase:latest
    container_name: metabase
    profiles: [dev, emulator]
    ports:
      - "3000:3000"
    environment:
      MB_DB_TYPE: h2


  # ── TEST profile ─────────────────────────────────────────────
  test-postgres:
    image: postgres:15-alpine
    container_name: abtest-test-postgres
    profiles: [test]
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d testdb -h 127.0.0.1 -p 5432"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s

  api-test:
    build: ./ab_test_platform
    profiles: [test]
    environment:
      DB_HOST: test-postgres
      DB_PORT: 5432
      DB_NAME: testdb
      DB_USER: postgres
      DB_PASSWORD: postgres
      ADMIN_EMAIL: admin@mail.ru
      ADMIN_FULLNAME: "Test Test"
      ADMIN_PASSWORD: "123123123aA!"
      RANDOM_SECRET: "Jf/ZpZSxfMWnOexP48Mp1z200jd+8BVZ7ws6Uw5Jp/w="
      SERVER_ADDRESS: "0.0.0.0:8080"
    volumes:
      - coverage-data:/coverage
      - ./ab_test_platform/coverage_html:/html
    depends_on:
      test-postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1/ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    command: ["sh", "/app/ab_test_platform/scripts/api_coverage.sh"]

  test-runner:
    build: ./ab_test_platform
    profiles: [test]
    environment:
      TEST_SERVER_URL: "http://api-test:8080"
    volumes:
      - coverage-data:/coverage
      - ./tests:/app/tests
    depends_on:
      api-test:
        condition: service_healthy
    command:
      - sh
      - -c
      - |
        pytest -o tavern-global-cfg=tests/config-docker.yml -v tests/
        EXIT=$?
        echo "done" > /coverage/pytest_done
        exit $EXIT

  # ── EMULATOR profile ────────────────────────────────────────
  emulator-postgres:
    image: postgres:15-alpine
    container_name: abtest-emulator-postgres
    profiles: [emulator]
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - emulator_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d testdb -h 127.0.0.1 -p 5432"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s

  emulator-api:
    build: ./ab_test_platform
    container_name: abtest-emulator-api
    profiles: [emulator]
    ports:
      - "8080:8080"
    environment:
      DB_HOST: emulator-postgres
      DB_PORT: 5432
      DB_NAME: testdb
      DB_USER: postgres
      DB_PASSWORD: postgres
    depends_on:
      emulator-postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1/ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  emulator:
    build: ./external_system_emulator
    container_name: abtest-emulator
    profiles: [emulator]
    ports:
      - "8090:8090"
    environment:
      AB_PLATFORM_URL: "http://emulator-api:8080"
      HOST: "0.0.0.0"
      PORT: "8090"
    depends_on:
      emulator-api:
        condition: service_healthy

volumes:
  postgres_data:
  coverage-data:
  emulator_pg_data:
